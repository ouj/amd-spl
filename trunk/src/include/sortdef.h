#include "algorithm"

namespace amdspl
{
    Sort::Sort(void)
    {
    }

    Sort::~Sort(void)
    {
    }

    template<class T> static 
        void Sort::BitonicSort(T *ptr, unsigned int _size)
    {
        
    }
}